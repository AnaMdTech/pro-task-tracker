export const FLOATING_BAR = {
    HEIGHT: 64,
    WIDTH: 280,            // Compact floating pill width (adjust to 300 if you want it slightly wider)
    HORIZONTAL_MARGIN: 20, // Used for FAB and card alignment (matches ScrollView padding)
    GAP_ABOVE_TAB: 16,     // Breathing room between the tab bar and floating elements
    BORDER_RADIUS: 32,
} as const;